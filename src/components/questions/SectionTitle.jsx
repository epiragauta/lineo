// ./src/components/questions/SectionTitle.js

import React from "react";

const SectionTitle = ({ title }) => {
  return (
    <h3 className="text-xl font-semibold mb-6">
      {title}
    </h3>
  );
};

export default SectionTitle;
